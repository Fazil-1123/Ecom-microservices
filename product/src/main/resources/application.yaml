spring:
  application:
    name: product-service
  profiles:
    active: ${ACTIVE_PROFILE:PROD}
  config:
    import: optional:configserver:http://localhost:8181

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8081



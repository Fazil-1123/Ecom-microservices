spring:
  application:
    name: user-service
  profiles:
    active: ${ACTIVE_PROFILE}
  config:
    import: optional:configserver:http://localhost:8181

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082